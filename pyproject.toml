[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "transformer-email-generation"
version = "0.1.0"
description = "Transformer models for email subject generation and continuation."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["utils*"]
